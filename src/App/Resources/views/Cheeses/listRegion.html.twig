<ul class="nav nav-pills nav-stacked">
    <li class="nav-header">Regions</li>
    {% for region in regions %}
    <li><a href="{{ path('index_cheeses_region', { region: region.region }) }}">{{ region.region }}</a></li>
    {% endfor %}
</ul>
